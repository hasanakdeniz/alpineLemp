services:
  mariadb:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
      MARIADB_USER: ${MARIADB_USER}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD}
      MARIADB_DATABASE: ${MARIADB_DATABASE}
    volumes:
      - mariadb:/var/lib/mysql
    networks:
      - test-tee-ov74uya

  alpine:
    build:
      context: .
      dockerfile: alpine.Dockerfile
      args:
        SFTP_USER: ${SFTP_USER}
        SFTP_PASSWORD: ${SFTP_PASSWORD}
    restart: always
    ports: 
      - 80
      - "${SFTP_PORT}:22"
    tty: true
    volumes:
      - alpine:/home/alpine/www
    networks:
      - test-tee-ov74uya
    labels:
      - traefik.http.routers.test-tee-ov74uya-20-web.rule=Host(`selam.pp.ua`)
      - traefik.http.routers.test-tee-ov74uya-20-web.entrypoints=websecure
      - traefik.http.routers.test-tee-ov74uya-20-web.tls=true
      - traefik.http.routers.test-tee-ov74uya-20-web.tls.certresolver=le
      - traefik.http.services.test-tee-ov74uya-20-web.loadbalancer.server.port=80
      - traefik.http.routers.test-tee-ov74uya-20-web.service=test-tee-ov74uya-20-web
      - traefik.docker.network=test-tee-ov74uya
      - traefik.enable=true

volumes:
  mariadb: {}
  alpine: {}

networks:
  test-tee-ov74uya:
    name: test-tee-ov74uya
